# El scope @naitre se resuelve contra Verdaccio (registry local del design system).
# Cuando se publique al registry definitivo, cambiar esta URL.
@naitre:registry=http://localhost:4873
auto-install-peers=true
